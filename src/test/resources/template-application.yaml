#spring:
#  datasource:
#    driverClassName: org.h2.Driver
#    url: jdbc:h2:mem:test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
#  jpa:
#    hibernate:
#      ddl-auto: validate

spring:
  datasource:
    url: jdbc:postgresql://[host]:[port]/[database_name]
    username:
    password:
    driverClassName: org.postgresql.Driver
    initialize: false
  jpa:
    hibernate:
      ddl-auto: validate
#    show-sql: true
  http:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
#logging:
#  path: /var/log/stuha-server

push:
  public:
  private:

awards:
  ranicek:
    conversationId:
    startHours: 5
    startMinutes: 30
    endHours: 11
    endMinutes: 00

messages.init.load.size: 10

token:
  revalidateAfter: 15
  invalidateAfter: 30